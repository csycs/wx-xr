export default definePageConfig({
  navigationBarTitleText: "AR 扫描",
  navigationStyle: "custom",
  disableScroll: true,
  renderer: "skyline",
  componentFramework: "glass-easel" // Skyline 渲染器需要
});
